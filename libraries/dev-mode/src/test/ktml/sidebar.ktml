import dev.ktml.SideBarItem

<sidebar @sideBarItems="${List<SideBarItem> = listOf()}">
    <div class="sidebar">
        <a each="${item in sideBarItems}" href="${item.href}">${item.name}</a>
    </div>
    <br>
</sidebar>