<card class="String = ''" header="Content? = null" headerClass="String = ''" content="Content">
    <div class="card $`class`">
        <div if="${header != null}" class="card-header $headerClass">
            ${header}
        </div>
        <div class="card-body">
            ${content}
        </div>
    </div>
</card>