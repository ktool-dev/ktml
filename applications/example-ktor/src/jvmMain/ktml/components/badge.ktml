<badge text="String" color="String? = null" rounded="Boolean = false" extraClass="String? = null">
    <span class="badge ${if (rounded) ' rounded-pill' else ''} bg-${color ?: 'primary'}${if (extraClass != null) ' ' + extraClass
    else ''}">${text}</span>
</badge>
