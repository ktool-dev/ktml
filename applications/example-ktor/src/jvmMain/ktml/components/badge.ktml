<badge text="$String" color="${String? = null}" rounded="${Boolean = false}" extraClass="${String? = null}">
    <span class="${buildClass('badge', rounded to 'rounded-pill', 'bg-${color ?: 'primary'}', extraClass)}">${text}</span>
</badge>
