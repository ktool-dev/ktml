<badge text="$String" color="${String? = null}" rounded="${Boolean = false}" extraClass="${String? = null}">
    <span class="${cssClass('badge', If(rounded, 'rounded-pill'), 'bg-${color ?: 'primary'}', extraClass)}">${text}</span>
</badge>
